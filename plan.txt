//PLAN//

sort into 2d array?
find a package that finds the similarity between two differnt strings?
if not, maybe try and code my own?

Damereau Levenshein Distance Algorithm returns a int of the differnce between two strings
https://en.wikipedia.org/wiki/Damerau%E2%80%93Levenshtein_distance

user types in 6 characters
check if 6 char are a valid hexadecimal



// CheckAgainstColours();
make a 2d array with 1 coloum being the 
return of the Compare() method
and the other being the iteration of i

check the lowest value in the array 
possibly using a for loop to cycle through the values?





